<%= link_to 'Назад', category_competency_path(@category, @competency), class: 'btn btn-primary' %>
<div class="container">
  <%= form_with model: [@category, @competency] do |f| %>
    <div class="row mb-2">
      <div class="col-9 form-group">
        <%= f.text_field :title, class: 'form-control' %>
      </div>
      <%= f.submit 'Сохранить изменения', class: 'btn btn-success' %>
    </div>
  <% end %>
</div>

<!--<div class="container">-->
  <%#= form_for([@category, @competency]) do |f| %>
<!--    <div class="row mb-2">-->
<!--      <div class="col-10 form-group">-->
        <%#= f.label :title, "Название компетенции" %>
        <%#= f.text_field :title, class: 'form-control' %>
<!--      </div>-->
      <%#= f.submit 'Сохранить изменения', class: 'btn btn-success' %>
<!--    </div>-->
  <%# end %>
<!--</div>-->