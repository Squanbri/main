<%= @category.title %>

<%= form_for([@category, @category.competencies.build]) do |f|%>
  <%= f.text_field(:title) %>
  <%= f.submit("Сохранить") %>
<% end %>

<% @category.competencies.each do |c| %>
  <%= c.title %><br>
<% end %>